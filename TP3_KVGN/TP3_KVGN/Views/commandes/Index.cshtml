@model IEnumerable<TP3_KVGN.Models.commande>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.date_rdv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user.login)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status.nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mode_paiement.nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.date_rdv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user.login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status.nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mode_paiement.nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_commande }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_commande }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_commande })
        </td>
    </tr>
}

</table>
